/*
*  main.js is the javascript file that will be used to populate the styleguidelines.html
*  page. It will read in a textfile filled with the information of the style guidelines.
*
*/

(function() {
   "use strict";

   const file_name = "support_files/styleguidelines/guideline-text.txt";

   //fields to the module
   let rules;



   window.addEventListener("load", init);

   /**
   *  Called when the page loads. Populates the page with information.
   */
   function init() {
      readFile(file_name);

   }

   function readFile(file) {
      7
   }









})();
